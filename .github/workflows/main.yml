name: Run my python
on:
    push:

jobs:
    build:
        name: do job1
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        - name: Run test py
          run: |
            python3 test.py
